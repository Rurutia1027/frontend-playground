// Ternary If Statement
var number = 2;
var result = number % 2 == 0 ? "Even" : "Odd";

// if the condition logic is too comples,
// we do not recommend us ethe ternary
// only the conditions are simple and easy to understand
// the we can use this expression to skip the if/else statements
console.log("result is " + result);
// if (number % 2 == 0) {
//   console.log("even");
// } else {
//   console.log("odd");
// }
